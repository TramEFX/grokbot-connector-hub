# Copy to data/.env or export before `python3 -m hub serve`
GCH_DATA_DIR=./data
GCH_HOST=127.0.0.1
GCH_PORT=8787
# Public URL Grok Bot will use (Tailscale / named tunnel). Leave blank for local.
GCH_PUBLIC_BASE=
GCH_ADMIN_TOKEN=
